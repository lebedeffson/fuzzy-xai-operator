model_id: papila_resnet50_binary
architecture: resnet50
pretrained: true
num_classes: 2
batch_size: 8
epochs: 20
early_stopping_patience: 5
optimizer:
  learning_rate: 0.0001
  weight_decay: 0.0001
selection_metric: validation_auroc
notes:
  - "Primary PAPILA model; ImageNet initialization is an external pretrained prior."
  - "This is a reproducible continuation of the PAPILA glaucoma/LIME line, not a replication of an author checkpoint."
