# External expert review infrastructure

The generated 100-object sample and blinded packets are produced by
`scripts/run_full_empirical_validation.py`. No result claim is allowed until
independent reviewer responses are stored separately and scored.

Current status: `planned_not_run`.
