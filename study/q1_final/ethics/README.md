# Ethics gate

Before recruitment, replace `not_started` with `approved` or `exempt` only after attaching a verifiable institutional
record. A public metadata record may replace a confidential full document, but it must identify the issuing body,
decision date and integrity hash.
