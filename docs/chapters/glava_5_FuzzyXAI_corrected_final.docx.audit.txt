# Глава 5. Audit text export

Статус: текстовый экспорт для автоматической проверки чисел и терминов до фиксации финального DOCX.

HYBRID-XIRIS проверяется как полный контрольный прогон. Значения берутся из исполняемого operator engine, proof package и экспортированных таблиц, а не набираются вручную.

Контрольный результат HYBRID-XIRIS:
- `γ = 0,351`;
- `Δ = 0,106811`;
- `ρ = 0,800`;
- `χ_R^{crit} = 1`;
- диагностическое состояние: `D_quality_source_conflict`;
- итоговое действие: блокировка.

Критические пропуски: базовый режим — 168, маршрут FuzzyXAI — 0.

GIS INTEGRO использует контрольную формулу:
`γ_route = max(|p - α_mean|, |p - s|)`,
где `p = 0,67`, `α_mean = 0,72`, `s = 0,47`, поэтому `γ_route = 0,20`.

Ограничения: сценарии не трактуются как промышленная или клиническая сертификация. HYBRID-XIRIS имеет уровень доказательности `full_control_run`; GD-ANFIS/SHAP и GIS INTEGRO являются `operator_control_example`; BEACON-XAI является `route_demonstration`.
