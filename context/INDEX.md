# Context index

Read `CURRENT_STATE.md` and `ACTIVE_TASK.md` for framework work. Read
`PROJECT.md`, `RESEARCH.md`, and `DECISIONS.md` only for dissertation scope,
protocol constraints, or a decision they specifically govern.

- `SCIENTIFIC_INVARIANTS.md`: semantic hard constraints for P19+.
- `ARCHITECTURE_MAP.md`: live framework operator route.
- `VALIDATION_MAP.md`: targeted and milestone validation routes.
- `CLAUDE_HANDOFF.md`: factual handoff observed on disk.
- `CODEX_BOOTSTRAP.md`: minimal startup procedure.

Do not load historic changelogs or generated validation artifacts unless the
active task explicitly requires them.
