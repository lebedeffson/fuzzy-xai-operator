# H10-C7 active evidence implementation

The planner emits bounded read-only pytest probes only when candidate entropy
is non-trivial. A registered observation may rerank candidates, but Gold IDs
are forbidden from the observable manifest. Development and held-out reports
must separate metrics before and after evidence requests.
