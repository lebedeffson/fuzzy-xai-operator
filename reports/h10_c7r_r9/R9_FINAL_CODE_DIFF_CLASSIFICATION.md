# R9 final code-to-run classification

The final development replay was executed from the R9 implementation that was
subsequently committed as `e99595fbe245ccabe7ede78c90f818ae951cf3e8`.

After the replay, one unused experimental linear-pairwise helper and its unit
test were removed. The executed LambdaMART path, feature extraction, channel
rankings, compressor, fold split, model parameters, gate calculation, and
generated result files were unchanged. The final focused and full regressions
were run after that removal.

This classification does not promote the result. The final status remains
`H10_C7R_R9_DEVELOPMENT_NO_GO`, scientific result `NOT_EVALUATED`.
