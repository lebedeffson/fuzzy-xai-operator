# Sensitivity analysis

В диапазоне theta_rank от 0.3 до 0.5 доля различий действия F0/NAS остаётся не ниже 65.70%, а recall critical остаётся не ниже 1.00.

Если используется k_top > 5, расчёт опирается на сохранённый top-5 профиль как conservative proxy.
