# H10-C7R R10 causal recollection run 1

Run `30566975780` executed the locked ten-incident technical barrier on an
isolated GitHub-hosted runner. It did not perform development scoring.

Five incidents passed the causal runtime readiness audit. They contained all
six registered causal event kinds, preserved chronology, 20 file candidates,
and symbol pools between 126 and 188.

Five incidents failed closed:

- three launchers entered copied virtual environments and recursively inspected
  a pytest proxy while deriving a value shape;
- one collector-created traceback observation lacked an observation timestamp;
- one temporary source projection traversed an inaccessible nested `.tox`
  environment.

These are collector defects, not retrieval metric failures. Collector v5
excludes copied environments from profiling, avoids dynamic shape access for
arbitrary objects, timestamps explicit postprocess observations, and removes
copied environment trees before repository import.

Status:
`H10_C7R_R10_CAUSAL_DEVELOPMENT_RECOLLECTION_IN_PROGRESS`.

Scientific result: `NOT_EVALUATED`.

The technical barrier did not pass, all ten incidents remain in the locked
selection, and no held-out data were created or scored.
