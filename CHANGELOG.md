# Changelog

## 1.1.0rc1 - Explanation Experience candidate

- claim-centered `ExplanationGraph` with typed `ExplanationClaim` records;
- honest E0-E5 explanation levels and native/surrogate/missing channel disclosure;
- public `overview()`, `story()`, `inspect()`, and `audit()` result API;
- typed `ExplanationVisualSpec` separated from scientific evidence;
- focused Matplotlib and Plotly views instead of a mixed-metric dashboard;
- MATLAB explanation-story and percentile data-profile views from the same JSON;
- controlled object 85, native-rule ANFIS, and research-only medical similarity golden explanations;
- comprehension study protocol frozen as `planned_not_run`.

## 1.0.0rc1 - research framework completion

- unified public `FuzzyXAI.wrap(...).explain(...)` API;
- evidence contracts for data, training, rules, concepts, similar cases, and counterfactuals;
- object forgetting and rare-subtype averaging diagnostics;
- native/surrogate rule extraction and measured rule ablation;
- class concepts with explicit uncovered fractions;
- `ExplanationGraph` and user/expert/audit summaries;
- `ExplanationViewModel` 2.0 with Matplotlib, HTML, and MATLAB presentation;
- cross-model integration tests and controlled object-85 protocol;
- operator manifest expanded to 30 verified entries;
- generated DubnaXAI prototype quarantined from the framework branch.
