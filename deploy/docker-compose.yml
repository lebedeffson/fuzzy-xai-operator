version: "3.9"
services:
  fuzzyxai-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.api
    ports:
      - "8000:8000"
  fuzzyxai-studio:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.studio
    ports:
      - "8501:8501"
