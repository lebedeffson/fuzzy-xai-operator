{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": {
    "pattern": "^[A-Za-z0-9._/:@-]+@sha256:[0-9a-f]{64}$",
    "type": "string"
  },
  "description": "Incident IDs map to prebuilt, immutable runtime images. A repository key is accepted only when the selected split contains one incident from that repository. Tags without a digest are forbidden.",
  "minProperties": 1,
  "propertyNames": {
    "pattern": "^(?:[A-Za-z0-9_.-]+__[A-Za-z0-9_.-]+|[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+)$"
  },
  "type": "object"
}
