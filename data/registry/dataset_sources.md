# Dataset Sources

All practice datasets in this release are repository control artifacts.

They are used to demonstrate the FuzzyXAI operator route:

`dataset -> model/evaluator -> scenario input -> engine -> proof package -> UI`

They are not clinical, industrial biometric, production GIS, or production BEACON validation datasets.

